.text-center
  %h1
    =@user.username
  %p{style: "color:grey;"}
    =@user.email

  .underline

.bg-grey{style:"padding-top:0px;"}
  %h2.text-center{style:"padding-bottom:10px;"} My Favs
  -@user.favorites.each do |favorite|
    .col-xs-4
      =render "products/product_cards", product: Product.find(favorite.product_id)
  .clearfix